# primersearch settings (TOML syntax). Edit values to change
# the defaults used when a CLI flag is not supplied. CLI flags
# always override values from this file.

tm_threshold = 55.0
na_concentration_mm = 150.0
search_mode = "no_ambiguities"
target_coverage_pct = 50.0
max_ambiguities = 1
exclude_n = false
only_twofold = false
orientation = "forward"
three_prime_match = 0
add_spacer = true
